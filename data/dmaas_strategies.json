{
  "_meta": {
    "version": 1,
    "purpose": "Normative strategy theses + default required/optional slots + default acceptance rules. Loaded by data/dmaas_scaffold_briefs/*.json (each brief inherits and may override). Also embedded verbatim in the dmaas-scaffold-author managed agent's system prompt."
  },
  "hero": {
    "thesis": "Single dominant message anchored top of canvas. Use when the message itself is the lead — bold positioning, big news, a confident assertion. Headline is the visual anchor; subhead and CTA support but never compete. No proof artifacts, no offer block.",
    "default_required_slots": ["headline", "cta"],
    "default_optional_slots": ["subhead", "background"],
    "default_acceptance_rules": [
      {"type": "size_hierarchy", "larger": "headline", "smaller": "subhead", "min_ratio": 1.5},
      {"type": "size_hierarchy", "larger": "headline", "smaller": "cta", "min_ratio": 1.2}
    ]
  },
  "proof": {
    "thesis": "Trust-building via credentials. Use when the audience needs to believe before they care. Headline frames; proof points (logos, stats, or testimonial) carry the credibility load. CTA is restrained, not the visual focus.",
    "default_required_slots": ["headline", "proof_strip", "cta"],
    "default_optional_slots": ["subhead", "background"],
    "default_acceptance_rules": [
      {"type": "slot_present", "slot": "proof_strip"},
      {"type": "min_slot_count", "category": "proof", "min": 1}
    ]
  },
  "offer": {
    "thesis": "Big-number offer is the visual anchor. Use when the offer itself is the most compelling thing — discounts, rebates, fixed-price, time-bound. Offer block dominates by area; headline frames it; CTA closes.",
    "default_required_slots": ["headline", "offer_block", "cta"],
    "default_optional_slots": ["subhead", "urgency_strip", "background"],
    "default_acceptance_rules": [
      {"type": "area_dominance", "element": "offer_block", "min_ratio_vs_others": 1.5}
    ]
  },
  "trust": {
    "thesis": "Authority and tenure signal. Use when 'we've been around' or 'we know your industry' matters more than features. Established voice, calm hierarchy, restrained palette. No urgency artifacts, no discount language. A credential strip (years in business, industry tenure) is mandatory.",
    "default_required_slots": ["headline", "credential_strip", "cta"],
    "default_optional_slots": ["subhead", "contact_block", "background"],
    "default_acceptance_rules": [
      {"type": "slot_present", "slot": "credential_strip"}
    ]
  }
}
